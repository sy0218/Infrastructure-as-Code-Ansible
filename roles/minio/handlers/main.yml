# -----------------------------------------------------
# minio 재시작 핸들러 → 환경 파일 / systemd 유닛 / 바이너리 링크 변경 시에만 실행
# -----------------------------------------------------
- name: restart minio
  systemd:
    name: minio
    state: restarted
    daemon_reload: true # 유닛 파일 변경분 반영
